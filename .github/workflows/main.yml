name: PR Open

on:
  push:
    # types: [opened, reopened, synchronize]
    paths:
      - 'src/**'

jobs:
  validate-changes:
    runs-on: ubuntu-latest
    container:
      image: salesforce/cli:latest-full
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      # - name: Install ESLint and Plugins
      #   run: |
      #     npm install eslint --save-dev
      #     sf plugins install @salesforce/eslint-plugin-lwc
      #     npx eslint --init

      - name: Run ESLint
        run: |
          echo "deploy-test-branch"
          chmod +x ./scripts/reporting.sh
          ./scripts/reporting.sh

#      - name: Deploy Lookup Tables
#        run: |
#          chmod +x ./scripts/newMatrixVersion.sh
#          ./scripts/newMatrixVersion.sh


